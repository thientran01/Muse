{
  "name": "muse-e2e-fixture",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "//": "Never installed. This file exists so museCore's detectStrategy() sees a Tailwind project (it reads deps as text) and so Vite treats this directory as a package root.",
  "devDependencies": {
    "tailwindcss": "^3.4.6"
  }
}
